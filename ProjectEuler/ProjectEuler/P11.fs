module P11
//
//open System
//
//let input = "test"
//// Convert input into rows of sequences of 30 Int64's
////let result =
////    let ints = input.ToCharArray() |> Array.map(fun elem -> Int64.Parse(elem.ToString()))
////    let arr2D = array.CreateInstance(int64, [|20 20|])
////    ints |> Seq.mapi(fun index elem -> arr2D.[])
//
//
//let sequencesOfFours
//for rows = 0..19 do
//	for cols = 0..19 do
//		getHorizontalSequences(rows, cols)